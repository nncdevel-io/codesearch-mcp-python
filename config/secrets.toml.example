# Code Search MCP Server — example secrets config.
# IMPORTANT: chmod 600 this file. Never commit real credentials.

[secrets.main-app]
auth_type = "token"
token = "ghp_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX"

[secrets.internal-lib]
auth_type = "ssh_key"
ssh_key_path = "/etc/code-search/keys/internal-lib"
